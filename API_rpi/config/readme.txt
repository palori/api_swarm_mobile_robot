# Configuration

This folder consists of all configuration files used for the robots, and can be in different formats (txt, csv...).

The files `config.h` and `config.cpp` are the ones that need to be called to parse the information from that files.

Some tests can be found in the `tests` folder.