cmake_minimum_required(VERSION 2.8)
project( pc detect line )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( pc_line pc_detect_line.cpp )
target_link_libraries( pc_line ${OpenCV_LIBS} )